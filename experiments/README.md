# Experiments

This directory contains reproducible experiment configurations and outputs.

Suggested structure:

```text
configs/       versioned experiment configs
scenarios/     task and behaviour definitions
scripts/       experiment entry points
runs/          local run artifacts (gitignored)
results/       processed results (only validated outputs)
```

No fabricated or hand-entered result data belongs here.
